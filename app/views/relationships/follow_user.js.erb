
      $("#follow-button")
        .text("Following")
        .attr("class", "btn unfollow-button")
        .attr("href", "/<%= @user.user_name %>/unfollow_user")
        .attr("id", "unfollow-button");
        $("#js-followers-count").html('<%= @user.followers.count %>');